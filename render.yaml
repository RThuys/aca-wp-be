services:
  - type: web
    name: backend
    env: node
    plan: free
    rootDirectory: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      # Add your Turso connection string and other secrets in Render dashboard.
      # - key: TURSO_DATABASE_URL
      #   value: "your-turso-connection-string"
